<h2>Upcoming Events</h2>
<% @upcoming.each  do |event|  %>
    <h3> <%= link_to event.name, event_path(event.id) %> </h3>
<% end %>

<h2>Past Events</h2>
<% @past.each  do |event|  %>
    <h3> <%= link_to event.name, event_path(event.id) %> </h3>
<% end %>